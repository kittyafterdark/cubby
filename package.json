{
  "name": "cubby-spindle-extension",
  "version": "0.1.2",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build src/frontend.ts --target=browser --format=esm --outfile=dist/frontend.js"
  },
  "devDependencies": {
    "lumiverse-spindle-types": "^0.6.23"
  }
}
